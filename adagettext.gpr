project Adagettext is

   for Object_Dir use "obj";
   for Source_Dirs use ("src");
   for Main use ("adagettext.adb");
   for Exec_Dir use "bin";

   package Naming is
      for Casing use "lowercase";
   end Naming;

   package Compiler is
   end Compiler;

   package Ide is
      for Documentation_Dir use "doc";
   end Ide;

   package Builder is
      for Default_Switches ("ada") use ("-s");
   end Builder;

end Adagettext;

